{
  "name": "ifrc-data-fetcher",
  "version": "1.0.0",
  "description": "Tool to fetch and process IFRC GO API data for PER dashboards",
  "main": "src/index.js",
  "scripts": {
    "fetch": "node src/fetchData.js",
    "process": "node src/processData.js \"./data\"",
    "check": "node src/checkData.js \"./data\"",
    "update-data": "npm run fetch && npm run process && npm run check"
  },
  "keywords": ["ifrc", "data", "api"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  }
}
